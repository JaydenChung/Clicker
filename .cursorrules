# LinkedIn Job Application Automation - Cursor Rules

---

## 🚨 CRITICAL RULES - READ FIRST

### THE CORE PRINCIPLE: AUTONOMOUS OPERATION
This is an **autonomous** job application agent. The user may leave their computer unattended expecting applications to continue. **A stopped session = missed opportunities.**

### RULE #1: NEVER STOP FOR "FIT" REASONS
**The session must NEVER stop because:**
- ❌ Job requires more experience than candidate has
- ❌ Salary seems too high/low for candidate
- ❌ Role seems too senior/junior
- ❌ Location isn't ideal
- ❌ Any "this isn't a good match" judgment

**If a suboptimal job is encountered**: Complete the application anyway, log concerns in notes, and continue to the next job. The agent applies - the human decides fit.

### RULE #2: SOFT BLOCKERS → LEAVE TAB OPEN, CONTINUE SESSION
When encountering verification requirements:
- ⏸️ Email verification codes
- ⏸️ Phone verification
- ⏸️ Account confirmation emails
- ⏸️ MFA/2FA prompts

**Action**: Log blocker → Leave tab open → Return to LinkedIn → Continue applying

### RULE #3: LOG EVERY SESSION START AND END
**Every session must be documented in `logs/session_stops.md`:**

**🟢 SESSION START:**
- Session ID, start timestamp, type (Easy Apply / External)
- `max_applications_per_session` from `/config/job_preferences.md`

**🔴 SESSION END:**
- End timestamp, duration
- Stop reason: Limit reached / Plan exhausted / Errors / User interrupt
- Applications completed count

**If you don't log it, the user can't debug or improve the system.**

### RULE #4: HARD BLOCKERS → SKIP APPLICATION, CONTINUE SESSION
- 🛑 Interactive CAPTCHA
- 🛑 Mandatory assessments
- 🛑 Video interview prerequisites

**Action**: Log blocker → Skip THIS application → Continue with next job

---

## Overview
This is an automated job application system for LinkedIn with **three main commands**:
1. **Plan Search** - Analyzes data and creates an optimal search strategy
2. **Apply to Jobs** - Executes Easy Apply applications based on the search plan
3. **Apply External** - Handles non-Easy Apply jobs on external websites

## Quick Start Commands

### Command 1: Plan Search
**Triggers**: "plan search", "what should I search", "search strategy", "analyze"

This command:
1. Reads application history from `/data/applications.csv`
2. Analyzes saturation by keyword + location
3. Generates a prioritized search plan
4. Saves plan to `/data/current_search_plan.md`

### Command 2: Apply to Jobs (Easy Apply)
**Triggers**: "apply to jobs", "start applying", "apply", "easy apply"

This command:
1. Reads the search plan from `/data/current_search_plan.md`
2. Reads `max_applications_per_session` from `/config/job_preferences.md`
3. Executes searches in priority order
4. Applies to **Easy Apply jobs only**
5. Updates CSV tracker after each application
6. Ends when `max_applications_per_session` reached or plan complete

### Command 3: Apply External
**Triggers**: "apply external", "external applications", "non-easy apply", "apply to external jobs"

This command:
1. Handles jobs that redirect to external company websites
2. Activates Application Director (supervisor) and External Applicant (executor)
3. Navigates various ATS systems (Workday, Greenhouse, Lever, etc.)
4. Handles open-ended questions with generated responses
5. Flags complex situations for human review

---

## Prerequisites Checklist
Before starting ANY session:
- [ ] Browser is open and controlled by Cursor
- [ ] LinkedIn is loaded and user is SIGNED IN
- [ ] Browser is on LinkedIn homepage or Jobs page
- [ ] Config files filled out in `/config/` directory
- [ ] For external apps: Resume file accessible for upload

---

## Agent Roles (9 Agents)

### 1. Search Strategist Agent (PLANNING)
**File**: `agents/search_strategist.md`
**Trigger**: "plan search" command
**Responsibilities**:
- Analyze application history
- Calculate saturation by keyword + location
- Generate prioritized search plan
- Predict optimal search combinations
- Learn from past session effectiveness

### 2. Job Applicant Agent (EASY APPLY EXECUTION)
**File**: `agents/job_applicant.md`
**Trigger**: "apply to jobs" command
**Responsibilities**:
- Read search plan from `data/current_search_plan.md`
- Execute searches in priority order
- Click "Easy Apply" on matching jobs
- Fill out application forms
- Submit applications
- Coordinate with other agents

### 3. Application Director Agent (EXTERNAL SUPERVISOR)
**File**: `agents/application_director.md`
**Trigger**: "apply external" command or when external site detected
**Responsibilities**:
- Analyze external page states
- Identify ATS systems (Workday, Greenhouse, etc.)
- Direct the External Applicant step-by-step
- Handle open-ended questions
- Decide skip vs. continue on blockers
- Track multi-page form progress

### 4. External Applicant Agent (EXTERNAL EXECUTOR)
**File**: `agents/external_applicant.md`
**Trigger**: Directed by Application Director
**Responsibilities**:
- Navigate external websites
- Fill form fields as directed
- Handle file uploads
- Execute clicks and selections
- Report results back to Director

### 5. CSV Tracker Agent (PERSISTENT DATA)
**File**: `agents/csv_tracker.md`
**Trigger**: After each successful application
**Responsibilities**:
- Maintain master CSV at `/data/applications.csv`
- Track ALL applications across ALL sessions
- Provide data for Google Sheets import
- Calculate statistics on demand

### 6. Search Logger Agent
**File**: `agents/search_logger.md`
**Trigger**: During application sessions
**Responsibilities**:
- Create session log in `logs/sessions/`
- Track searches performed this session
- Update search plan progress

### 7. Application Tracker Agent
**File**: `agents/application_tracker.md`
**Trigger**: After each successful application
**Responsibilities**:
- Create detailed MD log per application
- Update master index
- Complement CSV with rich details

### 8. Question Tracker Agent
**File**: `agents/question_tracker.md`
**Trigger**: When application questions are encountered
**Responsibilities**:
- Log all questions to `logs/questions/`
- Flag unanswered questions for human review
- Track open-ended questions from external applications

### 9. Performance Monitor Agent
**File**: `agents/performance_monitor.md`
**Trigger**: Throughout application process
**Responsibilities**:
- Track timing metrics
- Detect stuck states
- Log performance data
- Track external application times separately

---

## Workflow 1: Plan Search

```
┌─────────────────────────────────────────────────────────┐
│              COMMAND: "plan search"                      │
└─────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────┐
│                 LOAD DATA                                │
│  1. Read /data/applications.csv                         │
│  2. Read /config/job_preferences.md                     │
│  3. Read /config/locations.md                           │
│  4. Read /logs/sessions/ for recent activity            │
└─────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────┐
│              ANALYZE SATURATION                          │
│  - Count applications per keyword + location            │
│  - Identify saturated combinations (50+ apps)           │
│  - Identify fresh combinations (0-10 apps)              │
│  - Calculate days since last search                     │
└─────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────┐
│              GENERATE STRATEGY                           │
│  - Score each combination                               │
│  - Rank by priority                                     │
│  - Create search queue                                  │
│  - Save to /data/current_search_plan.md                 │
└─────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────┐
│              OUTPUT REPORT                               │
│  - Display saturation matrix                            │
│  - Show recommended search order                        │
│  - Highlight skip recommendations                       │
│  - Suggest session target                               │
└─────────────────────────────────────────────────────────┘
```

## Workflow 2: Apply to Jobs

```
┌─────────────────────────────────────────────────────────┐
│              COMMAND: "apply to jobs"                    │
└─────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────┐
│                 INITIALIZATION                           │
│  1. Verify browser on LinkedIn, signed in               │
│  2. Load /data/current_search_plan.md                   │
│  3. Load /config/personal_profile.md                    │
│  4. Load /config/job_preferences.md for max_applications│
│  5. LOG SESSION START to logs/session_stops.md          │
│  6. Create session log in logs/sessions/                │
│  7. Navigate to Jobs if needed                          │
└─────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────┐
│              SEARCH LOOP (from plan)                     │
│  FOR each search in current_search_plan:                │
│    1. Enter keyword in search box                       │
│    2. Enter location                                    │
│    3. Execute search                                    │
│    4. APPLY EXPERIENCE LEVEL FILTERS:                   │
│       ✅ Internship                                     │
│       ✅ Entry level                                    │
│       ✅ Associate                                      │
│    5. Mark search as in-progress in plan                │
│    6. → Application Loop                                │
│    7. Mark search as complete when done                 │
└─────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────┐
│              APPLICATION LOOP                            │
│  FOR each job with Easy Apply:                          │
│    1. Click job listing                                 │
│    2. Click Easy Apply                                  │
│    3. Fill form (from personal_profile.md)              │
│    4. Submit application                                │
│    5. Click Done                                        │
│    6. → Log to CSV Tracker                              │
│    7. → Log to Application Tracker                      │
│    8. → Update Performance Monitor                      │
└─────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────┐
│              SESSION END                                 │
│  Triggers:                                              │
│    - max_applications_per_session reached               │
│    - Search plan exhausted                              │
│    - Error threshold exceeded (5+ consecutive failures) │
│    - User interrupts                                    │
│  Actions:                                               │
│    - LOG SESSION END to logs/session_stops.md           │
│    - Save all data to CSV                               │
│    - Finalize logs                                      │
│    - Update search plan progress                        │
│    - Output session summary                             │
└─────────────────────────────────────────────────────────┘
```

## Workflow 3: Apply External (Non-Easy Apply)

```
┌─────────────────────────────────────────────────────────┐
│              COMMAND: "apply external"                   │
└─────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────┐
│                 INITIALIZATION                           │
│  1. Activate Application Director (supervisor)          │
│  2. Activate External Applicant (executor)              │
│  3. Load personal_profile.md for answers                │
│  4. Ensure resume file is accessible                    │
└─────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────┐
│              EXTERNAL APPLICATION LOOP                   │
│  FOR each non-Easy Apply job:                           │
│    1. Click "Apply" button on LinkedIn                  │
│    2. Handle redirect to external site                  │
│    3. Director analyzes page & identifies ATS           │
│    4. → Page Processing Loop                            │
└─────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────┐
│              PAGE PROCESSING LOOP                        │
│  WHILE not on confirmation page:                        │
│    1. Director snapshots & analyzes current page        │
│    2. Director identifies all form fields               │
│    3. FOR each field:                                   │
│       - Director provides value/instruction             │
│       - Executor fills field                            │
│       - IF open-ended question:                         │
│         → Generate response from templates              │
│         → Log to Question Tracker                       │
│    4. Click Next/Continue/Submit                        │
│    5. Wait for page load                                │
└─────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────┐
│              BLOCKERS & EDGE CASES                       │
│  ⏸️ SOFT BLOCKERS (Leave tab open, continue session):   │
│    - Email verification → Log, leave tab, continue      │
│    - Phone verification → Log, leave tab, continue      │
│    - Account email confirm → Log, leave tab, continue   │
│                                                         │
│  🛑 HARD BLOCKERS (Skip application, continue session): │
│    - CAPTCHA → Log, skip job, continue session          │
│    - Mandatory assessment → Log, skip, continue         │
│                                                         │
│  ⚠️ NOT BLOCKERS (Continue application):                │
│    - High experience requirements → Continue            │
│    - Salary mismatch → Continue                         │
│    - Senior-level role → Continue                       │
│                                                         │
│  Always log blockers in logs/session_stops.md           │
└─────────────────────────────────────────────────────────┘
                          │
                          ▼
┌─────────────────────────────────────────────────────────┐
│              COMPLETION                                  │
│  1. Detect confirmation page/message                    │
│  2. Log to CSV Tracker (mark as external)               │
│  3. Log to Application Tracker                          │
│  4. Record any flagged questions                        │
│  5. Return to LinkedIn for next job                     │
└─────────────────────────────────────────────────────────┘
```

### ATS System Reference

| ATS | URL Pattern | Complexity | Account Required |
|-----|-------------|------------|------------------|
| Workday | `*.myworkdayjobs.com` | High (4-7 pages) | Yes |
| Greenhouse | `boards.greenhouse.io` | Low (1-2 pages) | No |
| Lever | `jobs.lever.co` | Low (1 page) | No |
| Taleo | `*.taleo.net` | High (many pages) | Yes |
| iCIMS | `careers-*.icims.com` | Medium | Sometimes |
| SmartRecruiters | `jobs.smartrecruiters.com` | Medium | No |
| Custom | Company domain | Variable | Variable |

### Open-Ended Question Handling

When an open-ended question is encountered:

1. **Categorize** the question type:
   - "Why this company?" → Use company-specific template
   - "Describe experience with X" → Reference profile skills
   - "Salary expectations" → Use profile values
   - "Additional information" → Skip if optional

2. **Generate** appropriate response:
   - Keep concise (2-3 sentences usually)
   - Reference actual skills from profile
   - Never fabricate experience

3. **Log** to Question Tracker:
   - Mark confidence level
   - Flag for human review if needed

---

## Data Files

### Persistent Data (`/data/`)
```
/data/
├── applications.csv          # Master CSV - ALL applications ever
├── current_search_plan.md    # Active search strategy
└── search_insights.md        # Learning from past sessions
```

### Session Logs (`/logs/`)
```
/logs/
├── session_stops.md          # 🚨 MANDATORY - Log EVERY session end reason
├── sessions/                 # Per-session search logs
├── applications/             # Detailed per-application logs
├── questions/                # Question database
└── performance/              # Timing and stuck events
```

### Configuration (`/config/`)
```
/config/
├── job_preferences.md        # Keywords and filters
├── locations.md              # Target locations
├── personal_profile.md       # Personal info for forms
├── resume_content.md         # Detailed resume data for questions
└── projects.md               # Project portfolio for technical questions
```

---

## CSV Export for Google Sheets

The master CSV at `/data/applications.csv` can be imported to Google Sheets:

1. Download `applications.csv`
2. Google Sheets → File → Import → Upload
3. Select "Create new spreadsheet"
4. Separator: Comma

### Recommended Sheet Setup
- Freeze header row
- Add filters to all columns
- Conditional formatting for Status column
- Create pivot table for statistics

---

## User Commands Reference

| Command | What it does |
|---------|--------------|
| **Planning** | |
| "plan search" | Analyze data, generate search strategy |
| "what should I search" | Same as plan search |
| **Easy Apply** | |
| "apply to jobs" | Start Easy Apply using current plan |
| "apply to next X jobs" | Quick apply to X visible Easy Apply jobs |
| **External Applications** | |
| "apply external" | Apply to non-Easy Apply jobs (external sites) |
| "external applications" | Same as apply external |
| **General** | |
| "status" | Show current session statistics |
| "stop" | End session, save all data |
| "show csv stats" | Display application statistics |
| "export csv" | Confirm CSV location for download |

---

## Form Field Mapping

### Common Fields → Profile Keys

| Field Label Contains | Profile Section | Key |
|---------------------|-----------------|-----|
| "email" | Basic Information | email |
| "phone" | Basic Information | phone |
| "authorized" + "work" | Work Authorization | us_authorized → "Yes" |
| "sponsor" | Work Authorization | require_sponsorship → "No" |
| "salary" | Salary Expectations | desired_salary |
| "years" + [skill] | Experience Levels | [skill] |

---

## Error Recovery

### Application Dialog Won't Open
1. Wait 3 seconds
2. Scroll button into view
3. Retry click
4. If fails: Log error, skip job, continue

### Form Field Won't Accept Input
1. Click field to focus
2. Clear existing content
3. Type value
4. If fails: Log to stuck_log, try next field

### Rate Limit Detected
1. Log detection to performance monitor
2. Wait 30 seconds
3. Continue applying (do NOT end session)

---

## Session Settings

### Application Limit
- Read `max_applications_per_session` from `/config/job_preferences.md`
- Default: 10 (for testing)
- Set to 0 for unlimited
- Session ends when limit is reached

### On Session End (CRITICAL)
1. Save current application to CSV (if in progress)
2. Update search plan progress
3. Finalize all logs
4. Output summary with:
   - Applications submitted this session
   - CSV row count
   - Unanswered questions
   - Next recommended action

---

## Safety Rules (PRIORITY ORDER)

1. **🏆 COMPLETE APPLICATIONS NO MATTER WHAT** - Experience mismatches, salary, location are NOT reasons to stop
2. **📝 LOG SESSION START AND END** - Always document in `logs/session_stops.md` with timestamps
3. **⏸️ SOFT BLOCKERS: LEAVE TAB OPEN** - Don't close tabs with pending verifications, continue session
4. **💾 Always save to CSV** after each application (complete or blocked)
5. **🔒 Never delete CSV data** - only append
6. **📋 Log everything** for human review
7. **🔢 Respect application limit** - stop when `max_applications_per_session` reached

---

## Application Status Values

| Status | Description |
|--------|-------------|
| `Applied` | Successfully submitted |
| `Pending Manual` | Soft blocker - awaiting human completion (tab left open) |
| `Skipped` | Hard blocker - could not complete |
| `Interview` | Got interview (manual update) |
| `Rejected` | Rejected (manual update) |
| `Offer` | Received offer (manual update) |
| `No Response` | No response after X days (manual update) |

---

## After Each Session

The human should:
1. **Check `logs/session_stops.md`** → See why session ended and any pending actions
2. **Complete any "Pending Manual" applications** → Tabs should still be open
3. Review `logs/questions/unanswered.md` → Update `config/personal_profile.md`
4. Download `data/applications.csv` → Import to Google Sheets
5. Run "plan search" before next "apply to jobs" session
6. Check `logs/performance/stuck_log.md` for issues
